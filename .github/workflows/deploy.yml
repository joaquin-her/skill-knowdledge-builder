name: Deploy a GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

# Un solo deploy a la vez; no cancela el que ya está corriendo.
concurrency:
  group: pages
  cancel-in-progress: false

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - run: npx tsc --noEmit

      # basePath: GitHub Pages sirve los project sites bajo /<repo>, así que el
      # export necesita ese prefijo en rutas y assets.
      - name: Build estático
        run: npm run build
        env:
          NEXT_PUBLIC_BASE_PATH: /${{ github.event.repository.name }}

      # .nojekyll: sin esto Pages ignora los archivos que empiezan con guion
      # bajo, y Next emite justamente _next/.
      - run: touch out/.nojekyll

      - uses: actions/configure-pages@v4
      - uses: actions/upload-pages-artifact@v3
        with:
          path: out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
